Rollback validation note.

The `previous_safe` rollback option needs at least two recorded known-good releases before it can move back to an earlier rollback-safe release.

Use `recovery_mode` only after restoring a verified database backup.

Redeploy after rollback note.
